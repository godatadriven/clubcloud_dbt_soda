table_name: station_entrances
metrics:
  - row_count
  - missing_count
  - missing_percentage
  - values_count
  - values_percentage
  - invalid_count
  - invalid_percentage
  - valid_count
  - valid_percentage
  - avg_length
  - max_length
  - min_length
  - avg
  - sum
  - max
  - min
  - stddev
  - variance
tests:
  - row_count > 0
columns:
  route_8:
    valid_format: number_whole
    tests:
      - invalid_percentage == 0
  route_9:
    valid_format: number_whole
    tests:
      - invalid_percentage == 0
  route_10:
    valid_format: number_whole
    tests:
      - invalid_percentage == 0
  route_11:
    valid_format: number_whole
    tests:
      - invalid_percentage == 0
